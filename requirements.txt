# ВАЖНО: комментарии только на отдельных строках — некоторые хостинги
# читают файл построчно и передают строку в pip как есть.

# Раскрывающиеся цитаты (<blockquote expandable>) в карточках — это
# Bot API 7.4, в aiogram они появились в 3.7.
aiogram>=3.15,<4
aiosqlite>=0.20,<0.22
python-dotenv>=1.0,<2.0

# Зависимости движка дочерних ботов ставятся отдельно, из его
# requirements.txt: франшиза запускает его как чужую программу и в его
# зависимости не лезет.
