# ВАЖНО: комментарии только на отдельных строках — некоторые хостинги
# читают файл построчно и передают строку в pip как есть.

# Франшиза и движок живут в одном контейнере и делят один интерпретатор,
# поэтому здесь собраны зависимости обоих. Диапазоны — пересечение
# требований: у движка aiogram от 3.30 (цветные кнопки и премиум-эмодзи
# на кнопках), у франшизы — от 3.7 (раскрывающиеся цитаты).

aiogram>=3.30,<4.0
aiosqlite>=0.20,<0.21
python-dotenv>=1.0,<2.0

# Дальше — то, без чего движок не соберёт ни одного эмодзи: растр,
# шрифты, Lottie и векторные логотипы.
Pillow>=10.0,<13.0
fonttools>=4.0,<5.0
rlottie-python>=1.3.8,<2.0
svgelements>=1.9,<2.0
